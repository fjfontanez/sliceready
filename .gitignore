# OS / editor cruft
.DS_Store
**/.DS_Store
*.swp
.idea/
.vscode/

# Tooling caches / local state
.atl/

# Node
node_modules/
**/node_modules/

# Large mesh fixtures & generated outputs (never commit these)
*.stl
*.3mf
**/test/fixtures/*.stl
**/test/fixtures/*.3mf

# ADMesh source clone (fetched by spike/wasm/build.sh; pinned via ADMESH-SOURCE-COMMIT.txt)
**/admesh-src/

# Build and test output
dist/
test-results/
playwright-report/

# Agent worktrees (scratch, auto-managed)
.claude/worktrees/

# Local-only marketing / working docs (kept out of the repo on purpose)
docs/marketing/

# gga review contract — kept local so `gga run --pr-mode` still works, but not
# published (purged from history before the repo went public)
AGENTS.md
